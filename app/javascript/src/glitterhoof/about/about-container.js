import { connect } from 'react-redux';
import AboutComponent from './about-component';

const mapState = state => ({

});

const mapDispatch = dispatch => ({

});

export default connect( mapState, mapDispatch )( AboutComponent );
