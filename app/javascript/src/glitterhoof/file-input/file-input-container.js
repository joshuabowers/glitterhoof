import { connect } from 'react-redux';
import FileInputComponent from './file-input-component';

const mapState = state => ({

});

const mapDispatch = dispatch => ({

});

export default connect( mapState, mapDispatch )( FileInputComponent );
