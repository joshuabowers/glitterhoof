import { connect } from 'react-redux';
import NotFoundComponent from './not-found-component';

const mapState = state => ({

});

const mapDispatch = dispatch => ({

});

export default connect( mapState, mapDispatch )( NotFoundComponent );
