import { connect } from 'react-redux';
import SiteHeaderComponent from './site-header-component';

const mapState = state => ({

});

const mapDispatch = dispatch => ({

});

export default connect( mapState, mapDispatch )( SiteHeaderComponent );
